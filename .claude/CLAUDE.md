# HoangEditor.com Project

## GitHub Push Workflow

When the user says "push lên GitHub", "update GitHub", "sync code", or similar requests, run:

```bash
git add -A
git commit -m "<brief description of changes>"
git push origin main
```

Ask the user for a commit message if the changes aren't obvious.

### Repository Info
- Remote: `https://github.com/HoangEditor/Hoangeditor.com`
- Branch: `main`
- Local path: `/Users/Hoang/Documents/Hoangeditor.com/Hoangeditor.com`
